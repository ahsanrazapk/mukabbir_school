import 'package:flutter/material.dart';
import 'package:mukabbir_schools/api/api_calls.dart';
import 'package:mukabbir_schools/model/login_response.dart';

class LoginResponseProvider with ChangeNotifier {

  /*LoginResponse loginResponse = LoginResponse();
  bool loading = false;

  getLoginResponse(BuildContext context, String username, String password) async {

    loading = true;
    loginResponse = await ApiCalls.getLoginResponse(context, username, password);
    loading = false;

    notifyListeners();
  }*/
}